class Ball {
 
  private int x;
  private int y;
  private int width;
  private int height;
  private int speedX;
  private int speedY;
  
  public Ball() { }
  
  public Ball(int x, int y) {
    // complete the constructor
  }
  
  public Ball setWidth(int width) {
    // complete the function
  }
  
  public Ball setHeight(int height) {
    // complete the function
  }
  
  public void move(int leftWall, int rightWall, int topWall, int bottomWall) {
    if (/* hit a wall? hit the paddle? */) {
      
    }
    
    // move the ball
    
  }
  
  private boolean hit(int x, int y) {
    // complete the function
  }
}
